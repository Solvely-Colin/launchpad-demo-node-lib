name: Scheduled Maintenance
on:
  schedule:
    - cron: '0 9 * * 1'
  workflow_dispatch:

jobs:
  maintenance:
    uses: Solvely-Colin/solvely-launchpad/.github/workflows/scheduled.yml@v1
    with:
      package-manager: npm
